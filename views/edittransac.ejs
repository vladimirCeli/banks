<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.15.3/css/all.css"
      integrity="sha384-SZXxX4whJ79/gErwcOYf+zWLeJdY/qpuqC4cAa9rOGUstPomtqpuNWT9wdPEn2fk"
      crossorigin="anonymous"
    />
    <title>Caja de Ahorro</title>
  </head>
  <body>
    <br />
    <br />
    <% if (tipoTransaccion) { %>
    <div class="container mt-4">
      <div class="card border-secondary bg-dark">
        <div class="card-header text-white d-flex justify-content-center">
          Editar Registro Transaciones
        </div>
        <div class="card-body">
          <form action="/updatetransac" method="POST">
            <input
              type="id"
              name="id"
              value="<%= tipoTransaccion.id %>"
              hidden
            />
            <div class="mb-3">
              <label for="tipoTransaccion" class="form-label text-light"
                >Tipo de cuenta</label
              >
              <select
                name="tipoTransaccion"
                class="form-select"
                aria-label="Defaul select example"
                id="tipoTransaccion"
              >
                <option <%="tipoTransaccion.tipoTransaccion" %>
                  =='Deposito' ? "selected" : '' %> value="Deposito" >Deposito
                </option>
                <option <%="tipoTransaccion.tipoTransaccion" %>
                  =='Retiro' ? "selected" : '' %> value="Retiro">Retiro
                </option>
              </select>
            </div>
            <div class="mb-3">
              <label for="monto" class="form-label text-light">monto</label>
              <input
                type="number"
                class="form-control"
                id="monto"
                name="monto"
                value="<%= tipoTransaccion.monto %>"
              />
            </div>
            <div class="mb-3">
              <label for="descripcion" class="form-label text-light"
                >Descripción</label
              >
              <input
                type="text"
                class="form-control"
                id="descripcion"
                name="descripcion"
                value="<%= tipoTransaccion.descripcion %>"
              />
            </div>
            <div class="mb-3">
              <label for="idCuentaId" class="form-label text-light"></label>
              <select
                name="idCuentaId"
                class="form-select"
                aria-label="Defaul select example"
                id="idCuentaId"
              >
                <% resul.forEach((transac)=> { %>
                <option <%="tipoTransaccion.idCuentaId" %>
                  =='<%= transac.id %>' ? "selected" : '' %> value="<%=
                  transac.id %>"> <%= transac.propietario %>
                </option>
                <% }) %>
              </select>
            </div>
            <br />
            <div class="form-group d-flex justify-content-center gap-2">
              <button type="submit" class="btn btn-primary" tabindex="5">
                Guardar
              </button>
              <a href="/listran" class="btn btn-secondary">Cancelar</a>
            </div>
          </form>
        </div>
      </div>
    </div>
    <% } %>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
